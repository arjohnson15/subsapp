# Database Configuration
DB_HOST=db
DB_USER=dbuser
DB_PASSWORD=secure_password_change_me
DB_NAME=subsapp_db

# JWT Secret for authentication (if needed later)
JWT_SECRET=your_jwt_secret_change_me_to_something_random

# Email Configuration (Gmail)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Application Settings
NODE_ENV=production
PORT=3000

# Plex Server Configuration (optional - can be set via UI)
PLEX_SERVER_1_URL=
PLEX_SERVER_1_TOKEN=

PLEX_SERVER_2_URL=
PLEX_SERVER_2_TOKEN=

# Security Settings
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Backup Settings
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30